#include "main.h"
void flashcard(FlashcardSet *flashcard_set){
    bool star_only  =   false;
    bool shuffle_on =   false;

    WINDOW* mainPlayWindow = create_newwin(LINES-5, COLS-5, 2, 2);
    wbkgd(FlashcardWindow);
}
