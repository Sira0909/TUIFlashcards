#include <ncurses.h>
#include "../helpers.h"
#include "../window.h"
#include "../flashcards.h"
void play(char* list);


void flashcard(FlashcardSet *flashcard_set);

void type(FlashcardSet *flashcard_set);
